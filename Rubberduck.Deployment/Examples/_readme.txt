The folder contains samples useful for illustrating what are the possible inputs from heat.exe, and what our output should be like when writing to registry.

This is not actually used as part of building, nor is updated when building. 