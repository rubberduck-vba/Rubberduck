<UserControl x:Class="Rubberduck.UI.Settings.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resx="clr-namespace:Rubberduck.UI"
             xmlns:controls="clr-namespace:Rubberduck.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Border Grid.ColumnSpan="2" Height="50" VerticalAlignment="Top" BorderBrush="Black" BorderThickness="0, 1">
            <StackPanel Background="White">
                <Label Content="{x:Static resx:RubberduckUI.SettingsCaption_GeneralSettings}" FontWeight="Bold" />
                <TextBlock Text="{x:Static resx:RubberduckUI.SettingsInstructions_GeneralSettings}" Margin="10, 0" />
            </StackPanel>
        </Border>
        <ListBox Grid.Row="1" Grid.Column="0">
            <TextBlock>General</TextBlock>
            <TextBlock>Todo Items</TextBlock>
            <TextBlock>Inspections</TextBlock>
            <TextBlock>Unit Tests</TextBlock>
        </ListBox>
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Height="50" VerticalAlignment="Bottom" BorderBrush="Black" BorderThickness="0, 1, 0, 0">
            <DockPanel VerticalAlignment="Bottom" Height="50" Background="{x:Static SystemColors.ControlDarkBrush}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{x:Static resx:RubberduckUI.CancelButtonText}" MinWidth="75" Height="20" HorizontalAlignment="Right" Margin="5, 0" />
                    <Button Content="{x:Static resx:RubberduckUI.OK}" MinWidth="75" Height="20" HorizontalAlignment="Right" Margin="5, 0" />
                    <Button Content="{x:Static resx:RubberduckUI.Settings_ResetSettings}" MinWidth="75" Height="20"  HorizontalAlignment="Right" Margin="0, 0, 10, 0" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
