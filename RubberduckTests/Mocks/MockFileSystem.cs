using System.IO.Abstractions;

namespace RubberduckTests.Mocks
{
    // TODO: Actually implement the mock filesystem. For now, this uses the real filesystem.
    // The unit tests that uses filesystem will need to be updated to mock the filesystem
    // properly. 
    internal class MockFileSystem : FileSystem
    {

    }
}
